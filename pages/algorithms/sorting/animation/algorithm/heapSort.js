export function HeapSort(contextBackground, contextSwap, barsArray, speed, paused) {
    let bar1, bar2;
    let length = barsArray.length;
    let animationFrameIdArray = [];
    let i = 0

    // doInsertionSort()

    console.log("Heap Sort",contextBackground, contextSwap, barsArray, speed)

    return animationFrameIdArray
}

