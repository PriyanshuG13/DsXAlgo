export function QuickSort(contextBackground, contextSwap, barsArray, speed, paused) {
    let bar1, bar2;
    let length = barsArray.length;
    let animationFrameIdArray = [];
    let i = 0

    // doInsertionSort()

    console.log("Quick Sort",contextBackground, contextSwap, barsArray, speed)

    return animationFrameIdArray
}

